<form method="post" action="{{ path('app_item_catalog_delete', {'id': item_catalog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_catalog.id) }}">
    <button class="btn">Delete</button>
</form>
